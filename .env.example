# Copy this file and rename to ".env" to save your local configuration outside of version control.
# You should then be able to use the npm scripts that require these values.

# AWS Credentials (Optional)
# AWS_GOVCLOUD_PROFILE="default" # AWS CLI profile for GovCloud/main account deployments. Leave empty to use default credential chain (env vars, instance role, etc.)
# AWS_COMMERCIAL_PROFILE="commercial" # AWS CLI profile for commercial bridge deployments. Leave empty to use default credential chain.

# Deployment Mode (Optional)
# DEPLOYMENT_MODE="dev" # If set to "dev" deletion protection of stack resources is turned off.
# IS_GOVCLOUD="false" # Set to "true" when deploying to AWS GovCloud regions. This will use regional API Gateway and skip CloudFront deployment.

# Common
HUB_ACCOUNT_ID=000000000000 # The AWS account that the Compute and Data stacks will be deployed to.
NAMESPACE="myisb" # The namespace that the stacks will be deployed to.

# Account Pool Stack CFN Parameters
PARENT_OU_ID="ou-abcd-abcd1234" # The organization's root OU id is also acceptable.
AWS_REGIONS="us-east-1,us-west-2" # The regions enabled in the sandbox accounts.

# IDC Stack CFN Parameters
IDENTITY_STORE_ID="d-0000000000" # The Identity Store Id of the Identity Source in IAM Identity Center.
SSO_INSTANCE_ARN="arn:aws:sso:::instance/ssoins-0000000000000000" # The ARN of the SSO instance in IAM Identity Center.
IDC_REGION="us-east-1" # The AWS region where IAM Identity Center is deployed (e.g., us-east-1, us-gov-west-1).
IDC_KMS_KEY_ARN="AWS_OWNED_KEY" # The KMS key used by IAM Identity Center. Use "AWS_OWNED_KEY" for AWS-owned keys (default, no additional permissions needed), or provide the full ARN for customer-managed keys (e.g., arn:aws:kms:us-east-1:000000000000:key/00000000-0000-0000-0000-000000000000).
ADMIN_GROUP_NAME="" # Custom admin group name for IAM Identity Center instance, defaults to <namespace>_IsbAdmins if left empty.
MANAGER_GROUP_NAME="" # Custom manager group name for IAM Identity Center instance, defaults to <namespace>_IsbManagers if left empty.
USER_GROUP_NAME="" # Custom user group name for IAM Identity Center instance, defaults to <namespace>_IsbUsers if left empty.

# Compute Stack CFN Parameters
ORG_MGT_ACCOUNT_ID=000000000000 # The AWS account that the AccountPool stack is deployed to.
IDC_ACCOUNT_ID=000000000000 # The AWS account that the IDC stack is deployed to.
ACCEPT_SOLUTION_TERMS_OF_USE="" # Must be set to "Accept" for the compute stack to be deployed. Terms are located here ./source/infrastructure/lib/assets/terms-of-use.txt

# Configuration Overrides (Optional)
# NUKE_CONFIG_FILE_PATH="/example/path/nuke-config.yaml" # The file path for an alternative AWS Nuke configuration file to build the solution with.

# Private ECR Repo Configs (Optional)
# commands in package.json use HUB_ACCOUNT_ID for the account hosting ECR
# PRIVATE_ECR_REPO="myisb-account-cleaner" # The private ECR repo name for hosting the account cleaner image (defaults to <namespace>-account-cleaner).
# PRIVATE_ECR_REPO_REGION="us-east-1" # Region for the private ECR repo being used.
# PRIVATE_ECR_FRONTEND_REPO="myisb-frontend" # The private ECR repo name for hosting the frontend image for ECS deployment (defaults to <namespace>-frontend).

# Container Stack CFN Parameters (Optional)
# The Container stack deploys ECS-based services for account cleaner and frontend
# Required for GovCloud deployments (replaces CloudFront) or when using ECS-based frontend
# REST_API_URL="" # The URL of the backend REST API (from Compute stack output)
# ALLOWED_IP_RANGES="0.0.0.0/0" # Comma-delimited list of CIDR blocks allowed to access the frontend ALB
# VPC_CIDR="10.0.0.0/16" # The CIDR block for the Container stack VPC
# CERTIFICATE_ARN="" # ARN of ACM certificate for HTTPS on the ALB (optional, leave empty for HTTP only)

# Post-Deployment Stack CFN Parameters (Optional)
# The Post-Deployment stack automates IAM Identity Center SAML application setup
# Run this stack after deploying all other stacks
# WEB_APP_URL="" # The web application URL (CloudFront URL for commercial, ALB URL for GovCloud) - obtain from Compute or Container stack outputs

# Commercial Bridge Configuration (GovCloud Only)
# The Commercial Bridge provides access to commercial-only AWS APIs (Cost Explorer, Organizations CreateGovCloudAccount)
# COMMERCIAL_BRIDGE_API_URL="" # API Gateway URL from CommercialBridge-ApiGateway stack (auto-populated during deployment)
# ENABLE_PCA="false" # Set to "true" to create AWS Private CA (~$400/month). Alternative to self-signed certificates.
# PCA_CA_COMMON_NAME="Commercial Bridge Root CA" # Common Name for the PCA root certificate
# PCA_CA_ORGANIZATION="Innovation Sandbox" # Organization name for PCA
# PCA_CA_VALIDITY_DAYS="3650" # CA certificate validity in days (10 years default)
# ENABLE_ROLES_ANYWHERE="false" # Set to "true" to enable IAM Roles Anywhere for certificate-based auth
# GOVCLOUD_SECRET_NAME="/InnovationSandbox/CommercialBridge/ClientCert" # GovCloud Secrets Manager secret name for client certificate
# GOVCLOUD_REGION="us-gov-east-1" # GovCloud region where Secrets Manager secret is stored
# CERT_VALIDITY_DAYS="365" # Client certificate validity in days (1 year default)

# E2E Test Configs (only required to run E2E test suite) (Optional)
# DATA_STACK="InnovationSandbox-Data" # Name of the deployed Data stack.
# COMPUTE_STACK="InnovationSandbox-Compute" # Name of the deployed Compute stack.
# SANDBOX_ACCOUNT_ID="000000000000" # AWS account id to use for testing the account cleaner.
# SANDBOX_ACCOUNT_ADMIN_ROLE_NAME="Admin" # The name of the administrative role to use in the test account.
# EMAIL_FROM="email@example.com" # The email address to use for notifications during the e2e tests.
